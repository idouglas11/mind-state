// Minimal placeholder Toaster. No page in this app currently calls a
// useToast()/toast() hook, so this renders nothing but keeps the <Toaster />
// import in App.jsx working. Swap in shadcn's full toast system if you add
// toast triggers later.
export function Toaster() {
  return null;
}
